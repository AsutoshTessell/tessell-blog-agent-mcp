# Same variables as tessell-web/tessell-website (see sanity/lib/client.tsx).
# Copy this file to `.env` and fill in values. Do not commit `.env`.

SANITY_PROJECT_ID=
SANITY_DATASET=staging

# Optional: same read token as tessell-website if you use private dataset rules or need parity with local Next.
# SANITY_TOKEN=
